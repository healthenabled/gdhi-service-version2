server:
    port: 8888
spring:
  profiles: test
  datasource:
    url: jdbc:h2:mem:gdhi_test
    driverClassName: org.h2.Driver
    username: gdhi_test
    password: testpassword
    initialization-mode: always
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    hibernate:
      ddl-auto: create-drop
    show-sql: true
  h2:
    console:
      enabled: true
  flyway:
    baseline-on-migrate: true
    enabled: true
